
build:
  docker:
    frontend: ./install/docker/frontend/Dockerfile
    backend: ./install/docker/backend/Dockerfile
    redis: redis

  config:
    frontend:
      ports:
          - "4200:4200"
    backend:
      ports:
          - "5000:5000"
    redis:
      ports:
          -"6379:6379"
